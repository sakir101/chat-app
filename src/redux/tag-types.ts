export enum tagTypes {
    user = "user",
    admin = "admin",
    form = "form",
    message = "message"
}

export const tagTypesList = [
    tagTypes.user,
    tagTypes.admin,
    tagTypes.form,
    tagTypes.message
];